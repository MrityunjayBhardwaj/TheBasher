diff --git a/src/app/animate/ensureChannelForBone.ts b/src/app/animate/ensureChannelForBone.ts
--- a/src/app/animate/ensureChannelForBone.ts
+++ b/src/app/animate/ensureChannelForBone.ts
@@ -157,7 +157,7 @@
     const sorted = mine.slice().sort((a, b) => a.time - b.time);
     return sorted.map((k) => ({
       time: k.time,
-      value: component === 'rotation' ? radVec3ToDeg(k.rotation) : k.position,
+      value: component === 'rotation' ? k.rotation : k.position,
     }));
   }
   return [];
