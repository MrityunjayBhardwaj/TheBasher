public/assets/*.gltf
public/assets/*.glb
# Multi-file glTF fixtures (#110 Wave E Task 10) — generated by
# scripts/gen-multifile-gltf-fixture.mjs; the e2e gates on the exact
# committed bytes, so they MUST NOT be reflowed by prettier. Same
# rationale as public/assets/*.gltf above.
public/fixtures/multifile/
# Vendored Draco / Basis (KTX2) decoders — third-party JS+WASM wrappers
# copied from three-stdlib (`node_modules/three/examples/jsm/libs/...`).
# Not ours; do not reformat. See #80 / src/viewport/gltfLoaderConfig.ts.
public/draco/
public/basis/
VISOR.md

# Planning working-docs (phase CONTEXT/PLAN/SECTION-INVENTORY etc.) are
# working state, not shipped code — durable records live in memory. Not
# prettier-gated. (One tracked file slipped in via P7.4 W4 6ef2efa;
# this keeps it + all future .planning out of `prettier --check .`.)
.planning/
